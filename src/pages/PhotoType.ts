export interface FillterAgg {
  contrast: number,
  brightness: number,
  saturation: number,
  hue: number,
  r: number,
  g: number,
  b: number
  // saturation: number,
  // hue: number,
  // blur: number,
  // sharpen: number,
  // noise: number,
  // pixelate: number,
  // gamma: number,
  // invert: number,
}
