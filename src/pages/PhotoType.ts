export interface FillterAgg {
  contrast: number,
  brightness: number,
  saturation: number, 
  hue: number,
  // saturation: number,
  // hue: number,
  // blur: number,
  // sharpen: number,
  // noise: number,
  // pixelate: number,
  // gamma: number,
  // invert: number,
}
